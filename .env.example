# 复制这个文件为 .env，填入真实的值。.env 已经在 .gitignore 里，不会被提交。
#
# AI 的 key（Claude/GPT/DeepSeek/Gemini）不在这里配，是在 APP 里「聊天 → ⋯ → 设置」页
# 直接填的，存在手机本地安全存储里，跟这个 .env 没关系。

EXPO_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=xxx
